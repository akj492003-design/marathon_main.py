class Solution(object):
    def timeRequiredToBuy(self, tickets, k):
        time = 0
        for i in range(len(tickets)):
            if i <= k:
                contri = min(tickets[i], tickets[k])
            else:
                contri = min(tickets[i], tickets[k] - 1)
            time = time + contri
        return time
