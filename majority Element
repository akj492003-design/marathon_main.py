class Solution(object):
    def majorityElement(self, nums):
        m = {}
        n = len(nums)/2

        for num in nums :
            if num not in m:
                m[num] = 1
            else :
                m[num] += 1
        for num in nums :
            if m[num]> n:
                return num
